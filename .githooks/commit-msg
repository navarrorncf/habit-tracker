#!/bin/sh

set -u

repository_root=$(CDPATH= cd "$(dirname "$0")/.." && pwd)

if [ "$#" -ne 1 ]; then
    printf '%s\n' "usage: $0 <commit-message-file>" >&2
    exit 2
fi

exec "$repository_root/scripts/validate-git-conventions.sh" commit-message "$1"
